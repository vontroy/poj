#include <iostream>
#include <cstdio>

using namespace std;

int main() {
	int n;
	int array[150][150];

	for( int i = 0; i < n; i++ ) {
		for( int j = 0; j < n; j++ ) {
			scanf( "%d", array[i][j] );
		}
	}

	for( int i = 0; j < n; i++ ) {
		for( int j = i; j < n; j++ ) {
			dp[i][j] = dp[]
		}
	}
}